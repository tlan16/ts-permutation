import { permutationWithoutRepetition, permutationWithRepetition } from './lib';

const result = permutationWithoutRepetition(4);
// const result = permutationWithRepetition(4);
console.log({
  result,
  length: result.length,
});
